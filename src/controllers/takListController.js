module.exports = {
  // GET /app

  // GET /app/nova-lista

  // POST/app/nova-lista

  // GET /app/:id

  // POST /app/:id/excluir

  // POST /app/:id/nova-tarefa

  // POST /app/:listId/completar/:taskId
}